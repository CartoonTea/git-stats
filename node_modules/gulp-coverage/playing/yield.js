var myClass = function(index) {
        this.number = index;
    };

function* getClass(reset) {
    var index = 0;
    while(true) {
        if (reset) {
            index = 0;
        }
        yield new myClass(index++);
    }
}

var gen = getClass(true);
module.exports = gen;
